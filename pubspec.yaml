name: enough_convert
description: Support for ISO 8859 / Latin and Windows character encodings missing from `dart:convert`.
version: 0.9.0
homepage: https://github.com/Enough-Software/enough_convert

environment:
  sdk: '>=2.10.0 <3.0.0'

#dependencies:
#  characters: ^1.0.0

dev_dependencies:
  pedantic: ^1.9.0
  test: ^1.14.4
